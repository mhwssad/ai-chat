我需要创建一个监控模块， 用来统一各个模块的监控功能，

考虑把builtin_data使用json替代

异常统一使用config中的异常

我需要创建一个设置页面可以配置配置文件中的内容

chains模块：

- 异步方法缺少重试和超时机制（同步方法有）
- RAGChain 的关键词检索实现较简单（复用 similarity_search）
- metrics_collector 的 record_chain_call 函数不完整（latency_ms 硬编码为 0）
- 缺少结构化输出（JSON Schema 约束）
- 无 LangSmith 等外部追踪集成

创建定时任务模块

现在不需要阅读代码， 根据我的需求创建需求文档来确定项目的基调， 后续根据需求文档来继续实现， 当前没有需求文档实现过于分散，根据我提供的说明给我问题来精确访问
当前项目我打算制作成ai工具， 有正常的聊天功能， 支持多供应商， 支持聊天更换模型， 支持后续添加模型， 支持工具， mcp和skils调用和管理， 支持记忆功能， 支持web界面和cli界面， 完全使用异步功能来处理代码加快速度

我需要rag内容的管理， 如添加资料， 删除资料，查看资料， ai根据资料回答需要说明资料出处